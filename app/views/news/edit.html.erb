<% if current_user.organisation.id == @news.organisation_id %>
  <div class="row">
    <div class="col s12 m8 offset-m2 l8 offset-l2">
      <%@org_id = params[:org_id]%>
      <h1>Editing News</h1>
      <%= render 'form' %>
    </div>
  </div>
<% else %>
  <%= render "listings/forbidden" %>
<% end %>
