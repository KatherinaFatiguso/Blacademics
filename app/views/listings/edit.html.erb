<h1>Editing <%= @listing.listing_type %></h1>

<% if user_signed_in? %>
  <% if @organisation.user == current_user %>
    <%= render 'form' %>
    <%= link_to 'Show', @listing %> |
    <%= link_to 'Back', listings_path %>
  <% else %>
    <%= render 'forbidden' %>
  <% end %>
<% end %>
