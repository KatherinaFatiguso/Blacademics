<h1>Editing Organisation</h1>

<% if user_signed_in? %>
  <% if @organisation.user == current_user %>
    <%= render 'form' %>
    <%= link_to 'Show', @organisation %>
  <% else %>
    <%= render 'forbidden' %>
  <% end %>
<% end %>
